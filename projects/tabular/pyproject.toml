[project]
name = "tabular"
version = "0.1.0"
description = "Tabular Prediction with Autogluon"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "autogluon.tabular[all,skex,imodels,skl2onnx]>=1.5.0",
]

[dependency-groups]
dev = [
    "ipykernel>=6.30.0",
]
