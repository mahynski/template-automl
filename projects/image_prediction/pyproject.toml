[project]
name = "image-prediction"
version = "0.1.0"
description = "Image Prediction"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "autogluon.multimodal>=1.5.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
