[project]
name = "multimodal-prediction"
version = "0.1.0"
description = "Multimodal Prediction"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "autogluon.multimodal",
]

[dependency-groups]
dev = [
    "ipykernel>=6.30.0",
]
