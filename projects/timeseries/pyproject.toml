[project]
name = "timeseries"
version = "0.1.0"
description = "Timeseries Prediction with Autogluon"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "autogluon.timeseries",
    "prophet",
]

[dependency-groups]
dev = [
    "ipykernel>=6.30.0",
]
